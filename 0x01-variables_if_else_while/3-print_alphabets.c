#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 * Return: 0 always (success)
 */
int main(void)
{
	int ch = 'a';
	int CH = 'A';

	while (ch = 'a')
	{

		putchar(ch);
	}
	while (CH = 'A')
	{

		putchar(CH);
	}
	putchar('\n');
	return (0);
}
