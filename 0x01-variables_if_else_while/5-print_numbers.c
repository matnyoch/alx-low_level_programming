#include <stdio.h>
/**
 * main - Entry point
 * Description: A program will print all first nine digit
 * Return: Always 0 (success)
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
